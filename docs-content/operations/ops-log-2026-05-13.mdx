---
title: "Ops log — 2026-05-13 (Private)"
description: "What changed today across noddit.org, vaultwares-1, and the PC-hosted API. Private (tailnet-only)."
---

# Ops log — 2026-05-13 (Private)

> Private page (tailnet-only). Do **not** paste secrets here. Reference secret *names* and storage locations only.

## Summary / Résumé

**EN**
- `noddit.org` and `www.noddit.org` were brought online (public) on `vaultwares-1` with a real TLS certificate.
- The public site is a split reverse proxy:
  - `/` → Vault Flows SPA running on the PC
  - `/api/*` → VaultWares Pipelines API running on the PC
- The PC API gained a minimal in-process job queue + stricter job-submit rate limiting.

**FR-CA**
- `noddit.org` et `www.noddit.org` ont été mis en ligne (public) sur `vaultwares-1` avec un vrai certificat TLS.
- Le site public est un reverse proxy “split” :
  - `/` → SPA Vault Flows sur le PC
  - `/api/*` → API VaultWares Pipelines sur le PC
- L’API PC a reçu une file de jobs minimale (in-process) + un rate limit plus strict pour la soumission de jobs.

## Systems touched / Systèmes touchés

- **VPS (GreenCloud):** `vaultwares-1` (`173.249.194.15`, tailnet `100.73.93.84`, Debian 12)
- **PC (API host):** `Clopeux-Desktop` (tailnet `100.71.101.21`, Windows)
- **Domains:** `noddit.org`, `www.noddit.org`

## Changes on the VPS / Changements sur le VPS

### 1) Public TLS for noddit.org / TLS public pour noddit.org

**EN**
- Issued a Let’s Encrypt cert for `noddit.org` + `www.noddit.org`.
- Nginx vhost added:
  - Path: `/etc/nginx/sites-available/noddit.org.conf`
  - Enabled: `/etc/nginx/sites-enabled/noddit.org.conf`

**FR-CA**
- Certificat Let’s Encrypt généré pour `noddit.org` + `www.noddit.org`.
- Virtual host Nginx ajouté :
  - Fichier : `/etc/nginx/sites-available/noddit.org.conf`
  - Activé : `/etc/nginx/sites-enabled/noddit.org.conf`

### 2) Split reverse proxy (/ + /api) / Reverse proxy split (/ + /api)

**EN**
- `/` proxies to the SPA on the PC (`100.71.101.21:3100`) using upstream HTTPS (Vite preview).
  - Upstream TLS verification is currently disabled in Nginx because the PC uses a self-signed dev cert.
- `/api/` proxies to the PC API (`100.71.101.21:9001`) and injects:
  - `X-Forwarded-Proto: https` (so the API enforces HTTPS correctly behind the proxy)
  - `x-vw-gateway-secret: <secret>` (secret value stored outside docs)

**FR-CA**
- `/` proxy vers la SPA sur le PC (`100.71.101.21:3100`) via HTTPS upstream (Vite preview).
  - La vérification TLS upstream est désactivée pour l’instant (certificat dev auto-signé).
- `/api/` proxy vers l’API sur le PC (`100.71.101.21:9001`) et injecte :
  - `X-Forwarded-Proto: https` (pour que l’API traite l’appel comme HTTPS)
  - `x-vw-gateway-secret: <secret>` (valeur du secret stockée hors docs)

### 3) Websocket upgrade map / Map websocket upgrade

**EN**
- Added Nginx http-level map for upgrade handling:
  - Path: `/etc/nginx/conf.d/connection_upgrade.conf`

**FR-CA**
- Ajout d’un `map` Nginx (niveau `http`) pour gérer `Upgrade/Connection` :
  - Fichier : `/etc/nginx/conf.d/connection_upgrade.conf`

## Changes on the PC API host / Changements sur le PC (API)

### 1) API process state / État du process API

**EN**
- The API is intended to run on `0.0.0.0:9001` (tailnet reachable).
- Confirm the process is running before testing `noddit.org/api/*`.

**FR-CA**
- L’API doit rouler sur `0.0.0.0:9001` (accessible via tailnet).
- Confirmer que le process roule avant de tester `noddit.org/api/*`.

### 2) DB driver fix / Fix du driver DB

**EN**
- Installed missing Postgres driver used by Tortoise: `asyncpg`.

**FR-CA**
- Installation du driver Postgres manquant pour Tortoise : `asyncpg`.

### 3) Job queue + rate limiting / File de jobs + rate limiting

**EN**
- Added durable job records on disk + in-process worker queue:
  - `POST /workflows/run` now enqueues and returns `jobId` (status `queued`)
  - `GET /jobs` lists recent jobs
  - `GET /jobs/{jobId}` fetches job status/details
  - `POST /jobs/{jobId}/cancel` cancels a job (best-effort)
- Concurrency is capped (default `JOB_WORKER_CONCURRENCY=1`).

**FR-CA**
- Ajout d’un stockage durable des jobs sur disque + une file de workers (in-process) :
  - `POST /workflows/run` enfile un job et retourne un `jobId` (statut `queued`)
  - `GET /jobs` liste les jobs récents
  - `GET /jobs/{jobId}` retourne l’état/détails
  - `POST /jobs/{jobId}/cancel` annule un job (best-effort)
- La concurrence est limitée (par défaut `JOB_WORKER_CONCURRENCY=1`).

## Configuration notes / Notes de configuration

**EN**
- API HTTPS enforcement depends on trusted proxy CIDRs:
  - Set `TRUSTED_PROXY_CIDRS` to include the VPS tailnet IP (`100.73.93.84/32`) so `X-Forwarded-Proto` is trusted.
- `ALLOWED_ORIGINS` must include `https://noddit.org` and `https://www.noddit.org` for browser-initiated login.
- Keep `GATEWAY_REQUIRED_PUBLIC=1` and store `GATEWAY_SHARED_SECRET` in the secrets system (not in docs).

**FR-CA**
- L’application de “HTTPS required” dépend des CIDRs de proxy fiables :
  - Mettre `TRUSTED_PROXY_CIDRS` avec l’IP tailnet du VPS (`100.73.93.84/32`) pour faire confiance à `X-Forwarded-Proto`.
- `ALLOWED_ORIGINS` doit inclure `https://noddit.org` et `https://www.noddit.org` pour les logins via navigateur.
- Garder `GATEWAY_REQUIRED_PUBLIC=1` et stocker `GATEWAY_SHARED_SECRET` dans le système de secrets (pas dans docs).

## Verification / Vérification

**From the VPS / Depuis le VPS**
```bash
curl -kI https://noddit.org/
curl -kI https://noddit.org/api/
```

**From the PC / Depuis le PC**
```powershell
curl -sS -o NUL -w \"local_root=%{http_code}`n\" http://127.0.0.1:9001/
```

## Known follow-ups / Suivis connus

**EN**
- Replace the placeholder “job execution” with a real workflow runner.
- Remove upstream TLS verification bypass by serving the SPA upstream over HTTP (TLS terminates at VPS) or by using a verifiable internal cert.

**FR-CA**
- Remplacer l’exécution “placeholder” par un vrai runner de workflows.
- Retirer `proxy_ssl_verify off` en servant la SPA upstream en HTTP (TLS au VPS) ou via un certificat interne vérifiable.

