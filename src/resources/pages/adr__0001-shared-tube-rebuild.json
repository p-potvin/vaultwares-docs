{
  "EN": {
    "title": "ADR-001: Retire legacy tube sites; rebuild as Astro + vaultwares-api",
    "description": "Retire the legacy CMS install behind fullxxx.video + prom-king.xyz. Replace with an Astro monorepo (shared-tube engine + per-site apps) talking to a Prom-King router inside vaultwares-api. Hard cutover with a fresh catalog refetch. Adding a new site in the mix, the shared-tube should be a semi-replica of the layout of this site: https://www.fullvideos.xxx/sites/ (NOT the same domain as FXV)\". Ignore everything in this plan about a DNS swap, the new sites will directly replace the old ones, down time is not to be considered.",
    "body": "\n# ADR-001: Retire legacy tube sites; rebuild as Astro + vaultwares-api\n\n| Field             | Value                                                   |\n| ----------------- | ------------------------------------------------------- |\n| **Status**        | Approved                                                |\n| **Date**          | 2026-06-05                                              |\n| **Deciders**      | p-potvin (owner), Claude Opus 4.7 (architecture review) |\n| **Supersedes**    | —                                                       |\n| **Superseded by** | —                                                       |\n\n## Context\n\n`fullxxx.video` and `prom-king.xyz` were both legacy CMS installs driven by two custom plugins (`fullxxx-video`, `promking-tube`) and a `tube-shared` helper plugin. The catalog is small (≈ 270 active videos, 132 studios, 245 actors), the UX is read-heavy with a few islands of interactivity (player modal, search, consent banner, age gate, admin), and none of the legacy CMS headline features are in use: no built-in theme, no posts/pages editor for content, no comments, no media library, no plugin ecosystem, no Gutenberg, no XML-RPC, no users.\n\nWhat the WP install actually buys us today:\n\n- A relational store with a custom-post-type + taxonomy + term-meta abstraction\n- A cron loop\n- An admin UI we extended with bolted-on bulk actions and a Gender field\n- A rewrite-rules subsystem we keep fighting (the `/2257/ → year=2257` collision being the most recent)\n- A search hook stack\n- A sitemap generator we partially overrode\n\nEvery single one of these is replaceable in a few hundred lines of code.\n\nWhat the WP install costs us:\n\n- Two PHP versions on the box (8.2 for fpm, 8.4 for wp-cli) with extension mismatches that silently kill features (`mysqli`, `curl`, `xml` all bit us in the last 48 hours).\n- Deploy scripts that have to dance around `www-data` ownership, `vwdeploy` permissions, `flock` files, and wp-cli's \"YIKES, don't run as root.\"\n- Dev velocity tax: PHP/WP idioms (hooks, filters, template loaders, `WP_Query` arg surfaces, `rewrite_rules_array`) slow iteration when the operator is fluent in React/TypeScript.\n- Admin UX ceiling: every operator workflow we added (`fxv_actor_gender_*`, bulk reclassify, fetcher trigger) is grafted onto `wp-admin` and reads as exactly that — grafted on.\n- A second mental model for monitoring/logging — WP's own log surface vs. the rest of the VaultWares fleet (Health Ledger, Alarm Joker, Token Joker).\n\nThe owner has flagged two specific pain drivers: **dev velocity** and **operator UX**. Both point at the same conclusion: the WP overhead is paying for nothing we use, and the rebuild surface is small enough to be done in one stint.\n\n## Decision\n\nWe retire the legacy CMS installs at `fullxxx.video` and `prom-king.xyz` and replace them with a four-piece architecture:\n\n1. **`Prom-King/shared-tube`** — new repo, pnpm monorepo.\n2. **`shared/`** — npm package: design tokens, Astro components (header, footer, card, player, consent banner, age gate, ad-slot wrapper), fetcher engine (per-source XPath configs + normalize), Drizzle schema, shared TypeScript types.\n3. **`apps/fxv` + `apps/pkt`** — thin Astro apps. Hybrid SSR: legal pages and the home shell are pre-rendered at build time (SSG, served by nginx); catalog pages (listings, single video, taxonomy archives, search, admin) are SSR per request. Each app composes `shared/` with a per-site config (brand tokens, source list, GTM container, ad codes, legal text).\n4. **`vaultwares-api`** — extended with a new **Prom-King router** that owns: video/actor/studio/category CRUD, fetcher cron + manual trigger, settings, stats, auth integration (deferred enable). DB writes live here. Logging/monitoring stays uniform with the rest of the fleet.\n\nPostgres replaces MySQL. Drizzle replaces the WP query surface. React replaces the entire `wp-admin` extension layer for our workflows. Astro replaces the template loader + rewrite rule layer.\n\n**Migration is a hard cutover with a fresh catalog**: we drop the WP DB and refetch from the upstream sources after the new stack ships. The current catalog is small (≈ 270 videos) and refetchable, so the data-migration risk is preferable to one-shot ETL out of `wp_posts` + `wp_postmeta` + `wp_terms`.\n\n**Branch strategy**: all work happens on a feature branch in the new repo; the existing `Prom-King/tube-sites` WP install stays running until the new stack passes a local smoke test, then they replace the old ones.\n\n## Options Considered\n\n### Option A — Keep the legacy CMS, fix what's broken in place (do nothing)\n\n| Dimension          | Assessment                                                   |\n| ------------------ | ------------------------------------------------------------ |\n| Complexity         | Low (no rewrite)                                             |\n| Cost               | Low up front, accumulating tax                               |\n| Scalability        | Adequate for ≈ 1000 videos per site                          |\n| Team familiarity   | Already there                                                |\n| Maintenance burden | High — WP + PHP version drift, deploy script fragility, rewrite-rule edge cases |\n\n**Pros:** No rewrite. Catalog stays. Nothing to learn.\n\n**Cons:** Operator UX stays grafted-on. Dev velocity stays slow. Every new feature negotiates with WP's mental model. Monitoring stays bifurcated.\n\n### Option B — Headless CMS (Directus or Payload on Postgres) + Astro frontends\n\n| Dimension          | Assessment                               |\n| ------------------ | ---------------------------------------- |\n| Complexity         | Medium (two new components: CMS + Astro) |\n| Cost               | Low for CMS (free OSS), medium for setup |\n| Scalability        | Excellent                                |\n| Team familiarity   | New (CMS-specific quirks)                |\n| Maintenance burden | Medium (own + upgrade the CMS)           |\n\n**Pros:** Free admin UI on day one. Auto-generated REST/GraphQL. Clean schema separation.\n\n**Cons:** Owner explicitly disliked CMS UX — \"they're made for non-technical people, it makes it harder for me rather than simply coding what i want.\" **(Addendum: you can't quote someone if you're gonna rephrase in your own words bruh)** The page system, theming, shortcodes friction of WP would be replaced by similar friction in Directus. Doesn't address the operator-UX pain driver.\n\n**Rejected** on owner preference + the fact that the admin we need (bulk taxonomy edit, one-button fetcher, stats, canned SQL) is custom enough that a generic CMS would be in the way more than it would help.\n\n### Option C — Astro + Node API + Postgres, per-site backend (no central `vaultwares-api`)\n\n| Dimension          | Assessment                       |\n| ------------------ | -------------------------------- |\n| Complexity         | Medium                           |\n| Cost               | Low                              |\n| Scalability        | Good                             |\n| Team familiarity   | High (React/TS comfort)          |\n| Maintenance burden | Medium — two backends to monitor |\n\n**Pros:** Clean per-site boundary. Cheaper for the FXV/PKT scope alone.\n\n**Cons:** Bifurcates monitoring/logging from the rest of the VaultWares fleet. Auth would have to be re-invented per site or punted forever. Misses the synergy with `vaultwares-api` becoming the central API.\n\n**Rejected** in favour of Option D after the owner confirmed `vaultwares-api` is becoming the canonical API for every VaultWares + Prom-King app.\n\n### Option D — Astro frontends + Prom-King router in vaultwares-api + Postgres (chosen)\n\n| Dimension          | Assessment                                                   |\n| ------------------ | ------------------------------------------------------------ |\n| Complexity         | Medium — one new code surface (Astro) + one extension to existing API |\n| Cost               | Low (shares infra with the rest of the fleet)                |\n| Scalability        | Excellent                                                    |\n| Team familiarity   | High                                                         |\n| Maintenance burden | Low — one DB, one log stream, one auth surface ready to flip on |\n\n**Pros:** One API to monitor. One auth implementation. One DB connection pool. One logging story. Reusable design tokens (vault-flows). Owner velocity goes up because the iteration loop matches the rest of their fleet.\n\n**Cons:** `vaultwares-api` now hosts tube-specific business logic alongside everything else. Boundary is enforced by a `routes/promking/` subtree and a `db/promking/` schema. Worth the trade because the central monitoring story is what the owner explicitly named as the reason to do this.\n\n## Trade-off Analysis\n\nThe single most consequential trade-off is **Option B vs. Option D**: lean on someone else's admin (free, mediocre fit) or build our own (custom, exactly right). The owner is a technical operator who wants `console.log + button + bulk action + canned SQL`, not Gutenberg-style page composition. That's a 1–2 day build, not a 1–2 week CMS integration, and it pays for itself the first time we want to ship a workflow the CMS doesn't model cleanly.\n\nThe second consequential trade-off is **hard cutover vs. strangler fig**. Strangler is the textbook-safe answer for a real migration. Here, the catalog is small enough (≈ 270 videos), the refetch loop is automatic (pornxp + 1porn are already producing 60+ videos per run), and the legacy install is small enough that the cost of running parallel surfaces outweighs the cutover risk. Hard cutover means simpler code in v1 (no compatibility layer with `_fxv_*` postmeta keys) and no orphaned WP routes leaking into the new world.\n\nThe third trade-off is **Astro hybrid SSR vs. SSG**. Pure SSG buys the cheapest runtime but forces a build-on-fetch loop that gets fragile when the catalog refreshes hourly. Hybrid SSR gives static delivery for legal/home (which never changes between deploys) and live rendering for catalog (which always changes), without paying SSR cost for the bits that don't need it.\n\n## Consequences\n\n### What becomes easier\n\n- Every admin workflow is React behind `vaultwares-api` auth. Adding a new bulk action is one file under `apps/<site>/src/admin/`.\n- Adding a new source to the fetcher is one config block + one normalize function in `shared/src/fetcher/sources/`, no rewrite-rule dance, no detail-page extractor hooks unless the source actually needs them.\n- Stats, monitoring, and Health Ledger integration flow through `vaultwares-api` like every other VaultWares app.\n- React tokens / vault-flows components compose directly into the catalog.\n- Deploys are one `pnpm build` + one rsync per app. No PHP version dependency, no plugin dirs to chown.\n- Cutover **deletes ~5000 lines of PHP** that exist because WP needs them, not because the catalog does.\n\n### What becomes harder\n\n- We own the admin UX. No \"just install a plugin\" escape hatch. (We didn't have one with WP either, but the temptation will be gone.)\n- We own the auth surface (deferred enable, but the code path has to be designed in).\n- Search migrates from WP's `WP_Query` + custom filters to Postgres FTS. v1 lands the FTS index; tuning is a v1.1 problem.\n- Sitemap, JSON-LD, canonical, og/twitter meta all become explicit Astro components instead of WP filter chains. (Clearer, but more code in the diff.)\n\n### What we'll need to revisit\n\n- **Auth turn-on date.** Schema lands in v1 (users table + favourites + roles). UI lands when the owner says go.\n- **The PKT legacy CMS install** at `prom-king.xyz`. Same plan, same monorepo, parallel cutover. v1 ships both apps; the PKT-specific work (4 upstream sources, the wipe-and-rebackfill admin button) carries over via the same `shared/fetcher` engine with different configs.\n- **Health Ledger integration** for deploy alerts replaces the old GitHub-issue + `@copilot` pipeline (now disabled per `operations/deploy-alerts.mdx`).\n\n## Architecture\n\n### Top-level layout\n\n```\nProm-King/shared-tube/                    ← new repo\n├── pnpm-workspace.yaml\n├── package.json\n├── tsconfig.base.json\n├── shared/                               ← npm: @promking/shared-tube. layout, header, footer, categories, sneaky ads will come from https://www.fullvideos.xxx\n│   ├── package.json\n│   ├── src/\n│   │   ├── tokens/                       ← design tokens (palette, typography, spacing)\n│   │   │   ├── fxv.ts\n│   │   │   └── pkt.ts\n│   │   ├── components/\n│   │   │   ├── Header.astro\n│   │   │   ├── Footer.astro\n│   │   │   ├── VideoCard.astro\n│   │   │   ├── PlayerModal.astro         ← hls.js + native + unsupported fallback\n│   │   │   ├── ConsentBanner.astro\n│   │   │   ├── AgeGate.astro\n│   │   │   └── AdSlot.astro              ← banner / sidebar / inline / inplayer\n│   │   ├── fetcher/\n│   │   │   ├── engine.ts                 ← curl_multi-equivalent via undici\n│   │   │   ├── normalize.ts\n│   │   │   └── sources/\n│   │   │       ├── pornxp.ts\n│   │   │       ├── 1porn.ts\n│   │   │       ├── fullvideos.ts\n│   │   │       ├── xvideos.ts\n│   │   │       ├── xhamster.ts\n│   │   │       ├── spankbang.ts\n│   │   │       └── redtube.ts\n│   │   ├── db/schema.ts                  ← Drizzle: videos, actors, studios, categories, settings\n│   │   └── types/\n├── apps/\n│   ├── fxv/                              ← @promking/site-fxv\n│   │   ├── astro.config.mjs              ← static-default + per-page SSR opt-out (Astro 5; `hybrid` removed — see deviations)\n│   │   ├── src/\n│   │   │   ├── config.ts                 ← brand=fxv, sources=[pornxp,1porn,fullvideos.xxx], GTM, allowlist\n│   │   │   ├── pages/\n│   │   │   │   ├── index.astro           ← SSG (home shell + Hot/New rows from API at SSR time)\n│   │   │   │   ├── videos/index.astro    ← SSR: listing\n│   │   │   │   ├── videos/[slug].astro   ← SSR: single\n│   │   │   │   ├── actor/[slug].astro    ← SSR: actor archive\n│   │   │   │   ├── studio/[slug].astro\n│   │   │   │   ├── category/[slug].astro\n│   │   │   │   ├── search.astro          ← SSR with ?q=\n│   │   │   │   ├── 2257.astro            ← SSG (legal text)\n│   │   │   │   ├── dmca.astro            ← SSG\n│   │   │   │   ├── contact.astro         ← SSG\n│   │   │   │   ├── privacy-policy.astro  ← SSG\n│   │   │   │   ├── sitemap.xml.ts        ← SSR\n│   │   │   │   └── llms.txt.ts           ← SSG\n│   │   │   └── admin/                    ← React island, auth-gated\n│   │   │       ├── App.tsx\n│   │   │       ├── Catalog/Bulk.tsx\n│   │   │       ├── Fetcher/Console.tsx\n│   │   │       ├── Stats/Dashboard.tsx\n│   │   │       └── SQL/Panel.tsx\n│   │   └── public/\n│   └── pkt/                              ← same shape, brand=pkt, different sources\n└── README.md\n```\n\n### Data flow\n\n```\nAstro page (SSR)  ─── HTTPS ───►  vaultwares-api /api/promking/*\n                                       │\n                                       ├── routes/promking/videos.ts\n                                       ├── routes/promking/taxonomies.ts\n                                       ├── routes/promking/fetcher.ts   ───►  shared/fetcher (worker pool)\n                                       ├── routes/promking/settings.ts\n                                       └── routes/promking/stats.ts\n                                       │\n                                       ▼\n                                  Postgres (promking schema)\n```\n\n### DB schema (Drizzle, sketch)\n\n```ts\nvideos          (id, site, source, source_url, embed_url, embed_type,\n                 title, slug, thumbnail_url, preview_url, duration_seconds,\n                 views, created_at, updated_at)\nactors          (id, name, slug, gender, description, image_url)\nstudios         (id, name, slug, description, image_url)\ncategories      (id, name, slug, description)\nvideo_actors    (video_id, actor_id)\nvideo_studios   (video_id, studio_id)\nvideo_categories(video_id, category_id)\nfetch_runs      (id, site, source, started_at, finished_at, fetched, added, skipped, errors)\nsettings        (site, key, value JSONB)\nusers           (id, email, password_hash, role, created_at)  -- present but unused in v1\nfavourites      (user_id NULL, anon_id, video_id, created_at) -- localStorage-keyed for v1\n```\n\nPer-site rows are namespaced by `videos.site` (enum: `fxv`, `pkt`). Same table, single fetcher engine, site filter at the API layer.\n\n### Hosting\n\n- `apps/fxv` and `apps/pkt` deploy as standalone Node processes on `greencloud-vps` via `@astrojs/node` standalone, fronted by nginx for static asset cache + TLS.\n- `vaultwares-api` runs on `100.67.25.118:9001`; the new `/api/promking/*` routes deploy with the existing API deploy flow.\n- Postgres lives behind `vaultwares-api`; the current production target is\n  `vps-ovhcloud` alongside the API service.\n- Legacy CMS installs at `/var/www/fullxxx.video` and `/var/www/prom-king.xyz` are tarballed to `/var/backups/wp-archive-<date>.tar.gz` and removed right away.\n\n### Cutover plan\n\n1. New code lands on `Prom-King/shared-tube:main`. CI green.\n2. New apps deploy to `fullxxx.video` and `prom-king.xyz`, replacing the old code.\n3. WP install tarballed + deleted from `/var/www/`. Nginx vhost retired.\n4. Fetcher runs against the new DB; catalog repopulates from scratch.\n5. Owner smoke-tests on new deployments (home, listings, single video, search, taxonomy archives, admin, legal pages, GTM, consent, age gate, sitemap, JSON-LD).\n6. `Prom-King/tube-sites` repo deleted from the server and Clopeux-Desktop so as to not confuse anyone. (kept on github for history maybe; new work goes to `Prom-King/shared-tube`).\n\n## v1 scope (committed)\n\nAmbition level **#4 — aim wide**, including:\n\n- **Parity**: home (Hot/New/Studios rows), listings, single video, actor/studio/category archives, search, 4 legal pages, consent banner with GTM gate, age gate (localStorage-persistent), sitemap.xml, llms.txt, JSON-LD VideoObject, canonical/og/twitter meta. Draw HEEAVY inspiration from https://www.fullvideos.xxx\n- **New surfaces beyond parity**:\n  - Anonymous favourites (localStorage key + a `favourites` table keyed on a generated anon-id for cross-device when auth turns on).\n  - Postgres FTS search backend, with taxonomy term-name union (currently a `posts_search` filter hack in WP).\n  - Per-actor and per-studio gallery pages with thumbnail grids. This will be a copy of https://www.fullvideos.xxx/sites/ (careful, the domain is slightly different than ours)\n  - Custom admin: bulk-move/bulk-gender for taxonomies, one-button fetcher with a live console (server-sent events stream from `vaultwares-api`), stats dashboard (videos per source over time, fetch run history, dedupe ratio, per-video view counts), canned SQL panel with 8–10 pre-made queries.\n  - Visual ad-slot editor (drag rotation order, paste codes, preview gated by consent state).\n  - Per-site theming via design tokens — vault-flows palette extension for FXV/PKT.\n  - Health Ledger emit on every fetch run + deploy event.\n\nDeferred to v1.1:\n\n- Auth turn-on (schema is present in v1; UI ships when owner says).\n- Multi-language listings (catalogue is English-only today).\n- Per-site brand-token editor (tokens are TS files in v1; live editor is post-launch).\n\n## Action Items\n\n1. [x] Write this ADR.\n2. [x] Clone `Prom-King/shared-tube` locally. (`docs/` inside the repo links back to this ADR rather than duplicating it.)\n3. [x] Scaffold pnpm monorepo: `pnpm-workspace.yaml` + TS base config + ESLint + Prettier + Vitest. (Session 2026-06-05.)\n4. [x] `shared/` package skeleton: Drizzle schema + design tokens (fxv.ts + pkt.ts) + Astro component shells. **Real markup for components is still TODO** — shells only.\n5. [x] `apps/fxv` + `apps/pkt`: Astro 5 init with per-site config files. **See \"Implementation deviation: Astro 5 dropped `hybrid`\"** below.\n6. [x] `vaultwares-api`: branch `feat/promking-router` merged to `main` (2026-06-05, commit `66c9e3a`). Adds `app/routers/promking/{__init__,db,_models,videos,taxonomies,settings_routes,stats,fetcher,cron}.py`, mounted via `include_router` in `api_server.py` (try/except guarded so missing `PROMKING_DATABASE_URL` doesn't crash other routes). **Router lives on the FastAPI side**, calling the TS fetcher as a subprocess.\n7. [x] Fetcher port done — 7 sources (`pornxp` on `pornxp.bz`, `1porn` on `1porn.tv`, `fullvideos`, `xvideos`, `xhamster`, `spankbang`, `redtube`) live in `shared/src/fetcher/sources/`. `redtube` uses the new `customFetch` escape hatch (JSON API, no JSDOM). PKT URL/content reject patterns ported into `_filters.ts`. **Verified end-to-end 2026-06-06**: pornxp/1porn/fullvideos/xvideos/xhamster all persist real rows; spankbang returns 403 (Cloudflare); redtube API returns 0 items. Both broken sources are tracked as v1.1 follow-ups.\n8. [x] Fetcher wired. `POST /api/promking/fetcher/run` spawns the TS CLI; `GET /fetcher/stream/{run_id}` relays NDJSON → SSE; `GET /fetcher/runs` returns recent rows. APScheduler reads `settings.value->'schedules'` per (site, source) and registers cron jobs at startup. node-cron deviation logged.\n9. [x] Astro pages shipped both apps: home (SSR), 4 legal (SSG), `/videos/` listing (SSR), `/videos/[slug]` single with JSON-LD, taxonomy archives, `/search`, `/sitemap.xml.ts`, `/llms.txt.ts`. All pages config-agnostic; fxv and pkt share the same source tree (mirrored, not symlinked, so per-site overrides remain easy).\n10. [x] React admin at `/admin` per app — App shell with bearer-token sign-in against `/auth/me` (`vaultwares-api` exposes the verify endpoint under `/auth/`, not at root), Fetcher console with live SSE stream (uses `fetch + reader` because `EventSource` can't set custom headers), Catalog browser, Stats dashboard, SQL canned queries panel. Admin uses `window.location.origin` as API base — nginx proxies to `vaultwares-api` with the gateway secret injected (see deviation #9).\n11. [x] Deploy glue in `Prom-King/shared-tube/scripts/`: `deploy-shared-tube.sh` (honours every operations/deployment-flow invariant), `systemd/{fxv,pkt}.service`, `nginx/{fullxxx.video,prom-king.xyz}.conf` with `/admin` + SSE buffering disabled, `vw-webhookd.targets.yml` fragment, `deploy-vaultwares-api.patch.md` for the existing API deploy script, and a `scripts/README.md` bootstrap recipe.\n12. [x] **Local + prod smoke green.** Local: `pnpm -r build` clean, `db:generate` + `db:migrate` against `promking` produced 11 tables, `pnpm preview` served home/legal/llms.txt/sitemap.xml correctly. Prod (2026-06-06): both apps live with real letsencrypt certs; `https://fullxxx.video/api/promking/stats` returns shape-correct JSON; admin sign-in works; multiple fetcher runs persisted real rows. The two outstanding items — Lighthouse + GA gate — are operator polish, not blockers.\n\n## Locked decisions confirmed in session 2026-06-05\n\nThese were re-confirmed when scaffolding started so they don't need re-debating in future sessions:\n\n- **Router language**: Python (FastAPI), not the Node side of `vaultwares-api`. The TS fetcher engine in `shared/` is invoked as a subprocess from FastAPI (`shared/src/fetcher/cli.ts`); FastAPI relays the CLI's NDJSON stream as SSE to the admin console.\n- **Astro stack**: Astro 5 + React 19 + `@astrojs/node` standalone. (See deviation note below.)\n- **Workspace package scope**: `@promking/*` — `@promking/shared-tube`, `@promking/site-fxv`, `@promking/site-pkt`. Unpublished; workspace-only.\n- **Postgres**: same fleet Postgres host, **separate database `promking`** (not a schema inside `vaultwares`). Local dev: PostgreSQL 18 on the workstation, db created next to `vaultwares` with `CREATE DATABASE promking;`.\n\n## Implementation deviations from this ADR\n\nThese are intentional — anywhere this ADR's prose conflicts with what's in the repo, the repo wins. Listed here so reviewers don't trip on stale phrasing.\n\n1. **`output: 'hybrid'` → default `static` + per-page `prerender = false`.** Astro 5 removed the `hybrid` output mode; `static` is now the default and behaves identically. SSR pages (`videos/`, `videos/[slug]`, taxonomy archives, `search.astro`, `sitemap.xml.ts`, `/admin/*`) opt in with `export const prerender = false;`. Where this ADR says \"hybrid SSR\", read \"static-default + per-page SSR opt-out.\"\n2. **Fetcher CLI entrypoint.** The `shared/` file tree in this ADR doesn't show `shared/src/fetcher/cli.ts`. It exists and is the contract surface for FastAPI → fetcher (NDJSON over stdout). See the in-repo `docs/router-integration.md` for the wire format.\n3. **`fullvideos.xxx` selectors are now locked.** The ADR mentioned modeling after this site; the actual listing-page + detail-page selectors landed during scaffold based on owner-provided DOM notes. See `shared/src/fetcher/sources/fullvideos.ts` and `docs/fetcher-sources.md` — frontpage DMCA placeholders are filtered via `skipItemIf`.\n4. **`node-cron` → APScheduler.** The router lives on the Python (FastAPI) side of `vaultwares-api`, so the nightly fetch cron uses APScheduler in-process (`app/routers/promking/cron.py`) instead of node-cron. Schedule configuration is per-(site, source), stored in the `settings` table under key `fetcher_cron`, and reloadable without restart. Same intent as the ADR's \"node-cron for the scheduled run\" — just on the Python runtime.\n5. **`pornxp.com` → `pornxp.bz`** (was `.fo` in the PHP-port; `.fo` 301s to `.bz`, we pin `.bz` directly to skip the hop). **`1porn.com` → `1porn.tv`.** Anywhere the ADR or earlier docs say `.com`, the working host is the modern variant.\n6. **Apps run from `/srv/repos`, not `/var/www`.** The original cutover plan rsync'd `apps/<site>/dist/` into `/var/www/<domain>/` after `pnpm -r build` and pointed systemd `ExecStart` at that copy. `@astrojs/node` standalone externalises `react`/`react-dom`; without the workspace pnpm symlinks under `shared-tube/node_modules/.pnpm/...`, Node crashes with `ERR_MODULE_NOT_FOUND`. Systemd units now run from `/srv/repos/Prom-King/shared-tube/apps/<site>/dist/server/entry.mjs` with `WorkingDirectory=/srv/repos/Prom-King/shared-tube/apps/<site>/`. `/var/www/<domain>/` is no longer a deploy target. The deploy script no longer rsyncs.\n7. **xvideos is 1-indexed live**, contrary to the PHP-port's assumption of 0-indexed. `/new/0` now returns 404; `/new/1` is the first page. `Math.max(0, page-1)` was wrong; the source config now uses `page` directly.\n8. **`fullxxx.video` HTTPS via certbot, not pre-existing.** Greencloud-vps had a letsencrypt cert for `prom-king.xyz` but not `fullxxx.video` (the old WP install was broken externally anyway — both domains were already returning 404 before cutover). New cert obtained 2026-06-06 via `certbot certonly --webroot -w /var/www/_acme -d fullxxx.video -d www.fullxxx.video`. Auto-renewal scheduled.\n9. **Admin browser → API path is same-origin via nginx**, not direct-to-tailnet. The first admin design had a configurable API-base field defaulting to the API tailnet host. From an HTTPS page that's blocked by mixed-content + CORS. nginx now proxies `/api/promking/*` and `/auth/*` to `vaultwares-api`, injecting `x-vw-gateway-secret` from `/etc/nginx/snippets/vw-gateway-secret.conf` (0640 root:www-data; never committed). The admin React code uses `window.location.origin`; the override field was removed after stale localStorage values caused operators to keep hitting cross-origin URLs.\n10. **CLI emits one `videos` event per item, not one big array.** A 3-page `fullvideos` run produced a single ~80 KB `videos` line that blew past `asyncio.StreamReader`'s 64 KiB default. FastAPI's `readline()` silently failed and the persistence path never ran. The CLI now emits one `videos` event per item (single-element array each); FastAPI accumulates. As belt-and-braces, the subprocess `StreamReader` `limit` is raised to 10 MiB.\n11. **`_persist_videos` is per-row autocommit, no batch transaction.** The first design wrapped the batch in `async with conn.transaction():`. A single `NOT NULL` violation (e.g. `embed_url` empty on a fullvideos card with no `data-preview`) aborted the Postgres transaction; the inner `try/except` swallowed the immediate error but subsequent `INSERT`s all raised \"current transaction is aborted, commands ignored\", and the outer manager re-raised at commit — so `_finalize_run` never ran. Each row is now its own autocommit `INSERT … ON CONFLICT (site, source_url) DO NOTHING`. FastAPI also emits a `persisted` SSE event after persistence with the real `added` count, since the CLI's `done` always reports `added: 0`.\n12. **No public bearer fallback for the admin path.** `vaultwares-api` (FastAPI) gates `/auth/me`, `/auth/login`, `/auth/api-keys` against `BOOTSTRAP_ADMIN_*` in its `.env`. Tokens are minted via `POST /auth/login` (1h JWT) or `POST /auth/api-keys` (long-lived). The admin React app verifies against `/auth/me` (not `/me`) and persists the token in localStorage.\n\n## Verification\n\n- `pnpm -r build` green from a clean clone.\n- `pnpm -r test` green; ≥ 80 % coverage on fetcher normalize + DB schema constraints.\n- `curl https://fullxxx.video/` returns 200 with the Hot/New/Studios rows and < 200 ms TTFB.\n- Manual fetch trigger on `/admin/fetcher` yields ≥ 1 new video per active source (pornxp, 1porn), 0 errors.\n- Search for `sofia` returns matches across video titles and the `Sofia Smith` actor term.\n- `/sitemap.xml` contains every video URL + every taxonomy archive URL + every legal page.\n- `Location: https://fullxxx.video/2257/` returns 200 with the legal text (no redirect loop, ever).\n- Lighthouse: Performance ≥ 95, Accessibility ≥ 95, SEO ≥ 95 on the home page of both apps.\n- Health Ledger receives at least one fetch-run event per scheduled cycle, queryable by site.\n\n## Out of scope (explicit)\n\n- Rebuilding `link-sharing` or `prelanding-page` — those stay as-is.\n- Migrating `vaultwares-website` or `vault-flows` design system into shared-tube; No migration of VaultWares themes into Prom-King whatsoever.\n- Public auth UI (deferred per owner direction; schema ships, UI doesn't).\n- Visual brand-token editor (v1.1 polish).\n- Multi-language UI.\n"
  },
  "QC": null
}
